diff --git a/packages/client/ui-layout/src/client/AppFrame.module.css b/packages/client/ui-layout/src/client/AppFrame.module.css
index ae56780133..09041ea111 100644
--- a/packages/client/ui-layout/src/client/AppFrame.module.css
+++ b/packages/client/ui-layout/src/client/AppFrame.module.css
@@ -29,6 +29,10 @@
   border-right: 1px solid var(--dsw-alias-border-l1);
 }
 
+.sidebarBackdrop {
+  display: none;
+}
+
 .centerCol {
   min-width: 0;
   display: flex;
@@ -117,3 +121,51 @@
 .overlayLayer > * {
   pointer-events: auto;
 }
+
+/* Below the layout's auto-collapse breakpoint the conversation owns the full
+   grid width. The sidebar is an overlaid drawer while open and a transparent
+   carrier for the split floating control groups while closed. */
+@media (max-width: 1023px) {
+  .frame[data-floating-sidebar] .sidebarCol {
+    position: absolute;
+    z-index: 10;
+    top: max(8px, env(safe-area-inset-top));
+    bottom: max(8px, env(safe-area-inset-bottom));
+    left: max(8px, env(safe-area-inset-left));
+    min-width: 0;
+    max-width: calc(100% - 24px);
+    border: 1px solid var(--dsw-alias-border-l1);
+    border-radius: 20px;
+    background: var(--dsw-specific-sidebar-fill);
+    box-shadow: var(--dsw-shadow-lv3);
+    transition: width var(--ds-transition-duration-slow) var(--ds-ease-in-out);
+  }
+
+  .frame[data-floating-sidebar][data-sidebar-collapsed] .sidebarCol {
+    overflow: visible;
+    border: none;
+    border-radius: 0;
+    background: transparent;
+    box-shadow: none;
+    pointer-events: none;
+  }
+
+  .frame[data-floating-sidebar] .sidebarBackdrop {
+    position: absolute;
+    inset: 0;
+    z-index: 9;
+    display: block;
+    background: var(--dsw-alias-bg-mask-1);
+    backdrop-filter: var(--dsw-mask-blur);
+  }
+
+  .frame[data-floating-sidebar] .handle[data-side='sidebar'] {
+    display: none;
+  }
+}
+
+@media (max-width: 1023px) and (prefers-reduced-motion: reduce) {
+  .frame[data-floating-sidebar] .sidebarCol {
+    transition: none;
+  }
+}
diff --git a/packages/client/ui-layout/src/client/AppFrame.tsx b/packages/client/ui-layout/src/client/AppFrame.tsx
index 2696dc91fa..f965737fa2 100644
--- a/packages/client/ui-layout/src/client/AppFrame.tsx
+++ b/packages/client/ui-layout/src/client/AppFrame.tsx
@@ -143,6 +143,18 @@ export function AppFrame({
   const colsRef = useRef(cols)
   colsRef.current = cols
 
+  // Narrow viewports keep the conversation on the full frame and float the
+  // sidebar above it. Escape mirrors the backdrop gesture while the drawer is
+  // open; the compact rail stays click-through between its two control groups.
+  useEffect(() => {
+    if (!narrow || sidebarCollapsed) return
+    const onKeyDown = (event: KeyboardEvent): void => {
+      if (event.key === 'Escape') actions.toggleSidebar()
+    }
+    document.addEventListener('keydown', onKeyDown)
+    return () => { document.removeEventListener('keydown', onKeyDown) }
+  }, [actions, narrow, sidebarCollapsed])
+
   // The drag base is the rendered width captured at drag start (grabbing a
   // concession-clamped panel must not jump back to the stored preference);
   // it stays frozen for the whole gesture so dx deltas do not compound.
@@ -165,12 +177,16 @@ export function AppFrame({
     <div
       ref={frameRef}
       className={css.frame}
-      style={{ gridTemplateColumns: `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px` }}
+      style={{ gridTemplateColumns: `${narrow ? 0 : cols.sidebar}px minmax(0, 1fr) ${cols.details}px` }}
+      data-floating-sidebar={narrow || undefined}
       data-sidebar-collapsed={sidebarCollapsed || undefined}
       data-details-collapsed={cols.details === 0 || undefined}
       data-dragging={dragging || undefined}
     >
-      <div className={css.sidebarCol}>
+      {!sidebarCollapsed && narrow && (
+        <div className={css.sidebarBackdrop} role="presentation" onClick={() => { actions.toggleSidebar() }} />
+      )}
+      <div className={css.sidebarCol} style={narrow ? { width: cols.sidebar } : undefined}>
         {/* Render-site slot call with live concession output: a closed
             sidebar keeps the mounted slot at the compact-rail width, and the
             component sees its rendered state as owner params decided here
@@ -194,7 +210,7 @@ export function AppFrame({
         {renderSlot('shell.overlay', {})}
       </div>
       {/* The collapsed rail is fixed-width: no resize handle while closed. */}
-      {!sidebarCollapsed && <DragHandle side="sidebar" left={cols.sidebar} onStart={onSidebarStart} onDrag={onSidebarDrag} onEnd={onDragEnd} />}
+      {!narrow && !sidebarCollapsed && <DragHandle side="sidebar" left={cols.sidebar} onStart={onSidebarStart} onDrag={onSidebarDrag} onEnd={onDragEnd} />}
       {cols.details > 0 && <DragHandle side="details" left={viewport - cols.details} onStart={onDetailsStart} onDrag={onDetailsDrag} onEnd={onDragEnd} />}
     </div>
   )
diff --git a/packages/client/ui-layout/tests/app-frame.client.spec.tsx b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
index 65de5a640e..b934a59bec 100644
--- a/packages/client/ui-layout/tests/app-frame.client.spec.tsx
+++ b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
@@ -285,24 +285,41 @@ describe('AppFrame', () => {
 })
 
 describe('AppFrame — narrow-viewport auto-collapse', () => {
-  it('mounts collapsed below the breakpoint with no sidebar handle', () => {
+  it('mounts a floating rail over a full-width conversation below the breakpoint', () => {
     frameWidth = 980
     const { frame, slotCalls } = mountFrame()
-    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0])
+    expect(tracks(frame)).toEqual([0, 0])
+    expect(frame.hasAttribute('data-floating-sidebar')).toBe(true)
     expect(frame.hasAttribute('data-sidebar-collapsed')).toBe(true)
     expect(slotCalls.filter(c => c.key === 'sidebar').at(-1)!.props).toEqual({ collapsed: true, width: SIDEBAR_COLLAPSED })
+    expect((frame.firstElementChild as HTMLElement).style.width).toBe(`${SIDEBAR_COLLAPSED}px`)
     expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(0)
   })
 
-  it('narrow toggle re-expands over the squeezed center and back', () => {
+  it('narrow toggle opens an overlaid drawer without changing the center track', () => {
     frameWidth = 980
     const { frame, instance } = mountFrame()
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([0, 0])
     expect(frame.hasAttribute('data-sidebar-collapsed')).toBe(false)
-    expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(1)
+    expect((frame.querySelector('[class*="sidebarCol"]') as HTMLElement).style.width).toBe('280px')
+    expect(frame.querySelector('[class*="sidebarBackdrop"]')).not.toBeNull()
+    expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(0)
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0])
+    expect(tracks(frame)).toEqual([0, 0])
+  })
+
+  it('closes the floating drawer from its backdrop and Escape', () => {
+    frameWidth = 980
+    const { frame, instance } = mountFrame()
+    act(() => { instance.actions.toggleSidebar() })
+    const backdrop = frame.querySelector('[class*="sidebarBackdrop"]')!
+    act(() => { backdrop.dispatchEvent(new MouseEvent('click', { bubbles: true })) })
+    expect(frame.hasAttribute('data-sidebar-collapsed')).toBe(true)
+
+    act(() => { instance.actions.toggleSidebar() })
+    act(() => { document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true })) })
+    expect(frame.hasAttribute('data-sidebar-collapsed')).toBe(true)
   })
 
   it('a wide-closed preference re-expands at the contract default while narrow', () => {
@@ -312,7 +329,7 @@ describe('AppFrame — narrow-viewport auto-collapse', () => {
     frameWidth = 980
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([0, 0])
     expect(instance.getSnapshot().sidebar).toBe(0) // preference untouched
   })
 
@@ -321,7 +338,7 @@ describe('AppFrame — narrow-viewport auto-collapse', () => {
     act(() => { instance.actions.setSidebar(400) })
     frameWidth = 980
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
-    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0])
+    expect(tracks(frame)).toEqual([0, 0])
     frameWidth = 1920
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
     expect(tracks(frame)).toEqual([400, 0])
diff --git a/packages/client/ui-sidebar/src/client/SidebarRoot.module.css b/packages/client/ui-sidebar/src/client/SidebarRoot.module.css
index 300075b88d..329af2a55a 100644
--- a/packages/client/ui-sidebar/src/client/SidebarRoot.module.css
+++ b/packages/client/ui-sidebar/src/client/SidebarRoot.module.css
@@ -42,6 +42,13 @@
   --dsh-scrollbar-thumb-hover: transparent;
 }
 
+.primaryArea {
+  flex: 1;
+  min-height: 0;
+  display: flex;
+  flex-direction: column;
+}
+
 /* Collapse phase 1: the whole frozen-width content fades out in place over
    150ms; at settle the children unmount/snap to the rail layout. */
 .fading > * {
@@ -65,11 +72,12 @@
    render stays static. */
 .railIn .iconButton,
 .railIn .newSession,
-.railIn .regionArea {
+.railIn .regionArea,
+.railIn .footerActions {
   animation: rail-in 150ms var(--ds-ease-in-out) backwards;
 }
 
-.railIn .footArea {
+.railIn .settingsArea {
   animation: rail-fade-in 150ms var(--ds-ease-in-out) backwards;
 }
 
@@ -241,14 +249,8 @@
   padding-left: 0;
 }
 
-/* Footer seats: additive actions stack above Settings. Each occupant owns its
-   button geometry and hover chrome. */
-.footArea {
-  flex: none;
-  display: flex;
-  flex-direction: column;
-}
-
+/* Additive footer actions stay in the upper functional group; Settings owns
+   the independent bottom group. Each occupant owns its button geometry. */
 .settingsArea,
 .footerActions {
   flex: none;
@@ -260,10 +262,6 @@
   display: flex;
 }
 
-.collapsed .footArea {
-  align-items: center;
-}
-
 .collapsed .settingsArea,
 .collapsed .footerActions {
   display: flex;
@@ -271,13 +269,74 @@
   width: auto;
 }
 
+/* On narrow screens the layout places this root above a full-width
+   conversation. The compact rail therefore becomes two independent floating
+   surfaces: the functional controls at the top and Settings at the bottom. */
+@media (max-width: 1023px) {
+  .root {
+    border-radius: inherit;
+  }
+
+  .root.collapsed {
+    padding: 0;
+    overflow: visible;
+    background: transparent;
+    pointer-events: none;
+  }
+
+  .collapsed .primaryArea,
+  .collapsed .settingsArea {
+    width: 48px;
+    margin-inline: 4px;
+    box-sizing: border-box;
+    border: 1px solid var(--dsw-alias-border-l1);
+    border-radius: 18px;
+    background: var(--dsw-specific-sidebar-fill);
+    box-shadow: var(--dsw-shadow-lv2);
+    pointer-events: auto;
+  }
+
+  .collapsed .primaryArea {
+    flex: none;
+    min-height: 0;
+    padding: 6px;
+  }
+
+  .collapsed .logoRow {
+    margin-bottom: 6px;
+  }
+
+  .collapsed .newSession {
+    margin-bottom: 6px;
+  }
+
+  .collapsed .regionArea {
+    flex: none;
+    overflow: visible;
+  }
+
+  .collapsed .footerActions {
+    width: 36px;
+  }
+
+  .collapsed .settingsArea {
+    margin-top: auto;
+    padding: 6px;
+  }
+
+  .collapsed .settingsArea > button {
+    margin: 0;
+  }
+}
+
 @media (prefers-reduced-motion: reduce) {
   .wide,
   .fading > *,
   .railIn .iconButton,
   .railIn .newSession,
-  .railIn .footArea,
-  .railIn .regionArea {
+  .railIn .settingsArea,
+  .railIn .regionArea,
+  .railIn .footerActions {
     transition: none;
     animation: none;
   }
diff --git a/packages/client/ui-sidebar/src/client/SidebarRoot.tsx b/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
index d3fe6a63c1..6e43133bbb 100644
--- a/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
+++ b/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
@@ -127,65 +127,66 @@ export function SidebarRoot({
       }}
       onPointerLeave={() => { armLinger() }}
     >
-      <div className={css.logoRow}>
-        {/* Expanded, the wordmark doubles as a New Session shortcut; the
-            collapsed rail's logo is the expand toggle below instead. */}
-        {wide && (
+      <div className={css.primaryArea}>
+        <div className={css.logoRow}>
+          {/* Expanded, the wordmark doubles as a New Session shortcut; the
+              collapsed rail's logo is the expand toggle below instead. */}
+          {wide && (
+            <button
+              type="button"
+              className={clsx(css.brand, css.wide)}
+              aria-label={t('session.new.label')}
+              onClick={() => { startSession() }}
+            >
+              <BrandWordmark />
+            </button>
+          )}
+          {/* Rail resting state is the whale mark; hovering swaps in the panel
+              icon (the expand affordance, figma sidebar-hover flow). */}
+          <Tooltip label={collapsed ? t('toggle.open') : t('toggle.collapse')} delayMs={500}>
+            <button
+              type="button"
+              className={clsx(css.iconButton, css.toggle)}
+              aria-label={collapsed ? t('toggle.open') : t('toggle.collapse')}
+              onClick={() => { toggleSidebar() }}
+            >
+              {!wide && <FishLogo className={css.railFish} size={24} />}
+              {/* Rail icons render at 18 (figma rail spec); expanded keeps the glyph-native sizes. */}
+              <IconPanelLeftOutline16 className={css.panelIcon} size={wide ? 16 : 18} />
+            </button>
+          </Tooltip>
+        </div>
+
+        {/* Expanded, the button carries its own label — tooltip only on the rail. */}
+        <Tooltip label={t('session.new.label')} delayMs={500} disabled={wide}>
           <button
             type="button"
-            className={clsx(css.brand, css.wide)}
+            className={css.newSession}
             aria-label={t('session.new.label')}
             onClick={() => { startSession() }}
           >
-            <BrandWordmark />
-          </button>
-        )}
-        {/* Rail resting state is the whale mark; hovering swaps in the panel
-            icon (the expand affordance, figma sidebar-hover flow). */}
-        <Tooltip label={collapsed ? t('toggle.open') : t('toggle.collapse')} delayMs={500}>
-          <button
-            type="button"
-            className={clsx(css.iconButton, css.toggle)}
-            aria-label={collapsed ? t('toggle.open') : t('toggle.collapse')}
-            onClick={() => { toggleSidebar() }}
-          >
-            {!wide && <FishLogo className={css.railFish} size={24} />}
-            {/* Rail icons render at 18 (figma rail spec); expanded keeps the glyph-native sizes. */}
-            <IconPanelLeftOutline16 className={css.panelIcon} size={wide ? 16 : 18} />
+            <IconNewChatOutline16 size={wide ? 14 : 18} />
+            {wide && <span className={clsx(css.newSessionLabel, css.wide)}>{t('session.new')}</span>}
           </button>
         </Tooltip>
-      </div>
 
-      {/* Expanded, the button carries its own label — tooltip only on the rail. */}
-      <Tooltip label={t('session.new.label')} delayMs={500} disabled={wide}>
-        <button
-          type="button"
-          className={css.newSession}
-          aria-label={t('session.new.label')}
-          onClick={() => { startSession() }}
-        >
-          <IconNewChatOutline16 size={wide ? 14 : 18} />
-          {wide && <span className={clsx(css.newSessionLabel, css.wide)}>{t('session.new')}</span>}
-        </button>
-      </Tooltip>
-
-      {/* The browsing region fills the column between the controls and the
-          foot in both states; its rail icon column rides the same slot. */}
-      <div className={css.regionArea}>
-        {renderSlot('sidebar.workspaces', {
-          wide,
-          expandSidebar: () => { if (collapsed) toggleSidebar() },
-        })}
-      </div>
+        {/* The browsing region fills the column between the controls and the
+            footer actions; its rail icon column rides the same slot. */}
+        <div className={css.regionArea}>
+          {renderSlot('sidebar.workspaces', {
+            wide,
+            expandSidebar: () => { if (collapsed) toggleSidebar() },
+          })}
+        </div>
 
-      {/* Footer actions stack above Settings in both sidebar widths. */}
-      <div className={css.footArea}>
         <div className={css.footerActions}>
           {renderSlot('sidebar.footer.action', { wide })}
         </div>
-        <div className={css.settingsArea}>
-          {renderSlot('sidebar.settings', { wide })}
-        </div>
+      </div>
+
+      {/* Settings is the independent bottom control group in the mobile rail. */}
+      <div className={css.settingsArea}>
+        {renderSlot('sidebar.settings', { wide })}
       </div>
     </div>
   )
diff --git a/packages/client/ui-sidebar/tests/__snapshots__/sidebar-snapshot.client.spec.tsx.snap b/packages/client/ui-sidebar/tests/__snapshots__/sidebar-snapshot.client.spec.tsx.snap
index f82fbc1656..8605ac7bda 100644
--- a/packages/client/ui-sidebar/tests/__snapshots__/sidebar-snapshot.client.spec.tsx.snap
+++ b/packages/client/ui-sidebar/tests/__snapshots__/sidebar-snapshot.client.spec.tsx.snap
@@ -10,25 +10,43 @@ exports[`sidebar shell snapshots > renders the collapsed rail after the crossfad
     style=""
   >
     <div
-      class="logoRow"
+      class="primaryArea"
     >
+      <div
+        class="logoRow"
+      >
+        <button
+          aria-label="Open sidebar"
+          class="iconButton toggle"
+          type="button"
+        >
+          <svg
+            aria-hidden="true"
+            class="railFish"
+            data-content="965fe321"
+            fill="none"
+            height="17.6580310880829"
+            viewBox="0 0 23.16 17.04"
+            width="24"
+          />
+          <svg
+            class="panelIcon"
+            data-content="35f95b0c"
+            fill="none"
+            height="18"
+            viewBox="0 0 16 16"
+            width="18"
+            xmlns="http://www.w3.org/2000/svg"
+          />
+        </button>
+      </div>
       <button
-        aria-label="Open sidebar"
-        class="iconButton toggle"
+        aria-label="New session"
+        class="newSession"
         type="button"
       >
         <svg
-          aria-hidden="true"
-          class="railFish"
-          data-content="965fe321"
-          fill="none"
-          height="17.6580310880829"
-          viewBox="0 0 23.16 17.04"
-          width="24"
-        />
-        <svg
-          class="panelIcon"
-          data-content="35f95b0c"
+          data-content="c0ce4dcc"
           fill="none"
           height="18"
           viewBox="0 0 16 16"
@@ -36,49 +54,31 @@ exports[`sidebar shell snapshots > renders the collapsed rail after the crossfad
           xmlns="http://www.w3.org/2000/svg"
         />
       </button>
-    </div>
-    <button
-      aria-label="New session"
-      class="newSession"
-      type="button"
-    >
-      <svg
-        data-content="c0ce4dcc"
-        fill="none"
-        height="18"
-        viewBox="0 0 16 16"
-        width="18"
-        xmlns="http://www.w3.org/2000/svg"
-      />
-    </button>
-    <div
-      class="regionArea"
-    >
       <div
-        data-slot="sidebar.workspaces"
-        style="display: contents;"
-      />
-    </div>
-    <div
-      class="footArea"
-    >
-      <div
-        class="footerActions"
+        class="regionArea"
       >
         <div
-          data-slot="sidebar.footer.action"
+          data-slot="sidebar.workspaces"
           style="display: contents;"
         />
       </div>
       <div
-        class="settingsArea"
+        class="footerActions"
       >
         <div
-          data-slot="sidebar.settings"
+          data-slot="sidebar.footer.action"
           style="display: contents;"
         />
       </div>
     </div>
+    <div
+      class="settingsArea"
+    >
+      <div
+        data-slot="sidebar.settings"
+        style="display: contents;"
+      />
+    </div>
   </div>
 </div>
 `;
@@ -93,85 +93,85 @@ exports[`sidebar shell snapshots > renders the expanded column (wordmark, capsul
     style="width: 300px;"
   >
     <div
-      class="logoRow"
+      class="primaryArea"
     >
-      <button
-        aria-label="New session"
-        class="brand wide"
-        type="button"
+      <div
+        class="logoRow"
       >
-        <svg
-          aria-hidden="true"
-          data-content="951274b9"
-          fill="none"
-          height="24"
-          viewBox="0 0 182 24"
-          width="182"
-        />
-      </button>
+        <button
+          aria-label="New session"
+          class="brand wide"
+          type="button"
+        >
+          <svg
+            aria-hidden="true"
+            data-content="951274b9"
+            fill="none"
+            height="24"
+            viewBox="0 0 182 24"
+            width="182"
+          />
+        </button>
+        <button
+          aria-label="Collapse sidebar"
+          class="iconButton toggle"
+          type="button"
+        >
+          <svg
+            class="panelIcon"
+            data-content="35f95b0c"
+            fill="none"
+            height="16"
+            viewBox="0 0 16 16"
+            width="16"
+            xmlns="http://www.w3.org/2000/svg"
+          />
+        </button>
+      </div>
       <button
-        aria-label="Collapse sidebar"
-        class="iconButton toggle"
+        aria-label="New session"
+        class="newSession"
         type="button"
       >
         <svg
-          class="panelIcon"
-          data-content="35f95b0c"
+          data-content="c0ce4dcc"
           fill="none"
-          height="16"
+          height="14"
           viewBox="0 0 16 16"
-          width="16"
+          width="14"
           xmlns="http://www.w3.org/2000/svg"
         />
+        <span
+          class="newSessionLabel wide"
+        >
+          New Session
+        </span>
       </button>
-    </div>
-    <button
-      aria-label="New session"
-      class="newSession"
-      type="button"
-    >
-      <svg
-        data-content="c0ce4dcc"
-        fill="none"
-        height="14"
-        viewBox="0 0 16 16"
-        width="14"
-        xmlns="http://www.w3.org/2000/svg"
-      />
-      <span
-        class="newSessionLabel wide"
-      >
-        New Session
-      </span>
-    </button>
-    <div
-      class="regionArea"
-    >
-      <div
-        data-slot="sidebar.workspaces"
-        style="display: contents;"
-      />
-    </div>
-    <div
-      class="footArea"
-    >
       <div
-        class="footerActions"
+        class="regionArea"
       >
         <div
-          data-slot="sidebar.footer.action"
+          data-slot="sidebar.workspaces"
           style="display: contents;"
         />
       </div>
       <div
-        class="settingsArea"
+        class="footerActions"
       >
         <div
-          data-slot="sidebar.settings"
+          data-slot="sidebar.footer.action"
           style="display: contents;"
         />
       </div>
     </div>
+    <div
+      class="settingsArea"
+    >
+      <div
+        data-slot="sidebar.settings"
+        style="display: contents;"
+      />
+    </div>
   </div>
 </div>
 `;
@@ -186,85 +186,85 @@ exports[`sidebar shell snapshots > renders the expanded column in the default lo
     style="width: 300px;"
   >
     <div
-      class="logoRow"
+      class="primaryArea"
     >
-      <button
-        aria-label="新建会话"
-        class="brand wide"
-        type="button"
+      <div
+        class="logoRow"
       >
-        <svg
-          aria-hidden="true"
-          data-content="951274b9"
-          fill="none"
-          height="24"
-          viewBox="0 0 182 24"
-          width="182"
-        />
-      </button>
+        <button
+          aria-label="新建会话"
+          class="brand wide"
+          type="button"
+        >
+          <svg
+            aria-hidden="true"
+            data-content="951274b9"
+            fill="none"
+            height="24"
+            viewBox="0 0 182 24"
+            width="182"
+          />
+        </button>
+        <button
+          aria-label="收起侧边栏"
+          class="iconButton toggle"
+          type="button"
+        >
+          <svg
+            class="panelIcon"
+            data-content="35f95b0c"
+            fill="none"
+            height="16"
+            viewBox="0 0 16 16"
+            width="16"
+            xmlns="http://www.w3.org/2000/svg"
+          />
+        </button>
+      </div>
       <button
-        aria-label="收起侧边栏"
-        class="iconButton toggle"
+        aria-label="新建会话"
+        class="newSession"
         type="button"
       >
         <svg
-          class="panelIcon"
-          data-content="35f95b0c"
+          data-content="c0ce4dcc"
           fill="none"
-          height="16"
+          height="14"
           viewBox="0 0 16 16"
-          width="16"
+          width="14"
           xmlns="http://www.w3.org/2000/svg"
         />
+        <span
+          class="newSessionLabel wide"
+        >
+          新会话
+        </span>
       </button>
-    </div>
-    <button
-      aria-label="新建会话"
-      class="newSession"
-      type="button"
-    >
-      <svg
-        data-content="c0ce4dcc"
-        fill="none"
-        height="14"
-        viewBox="0 0 16 16"
-        width="14"
-        xmlns="http://www.w3.org/2000/svg"
-      />
-      <span
-        class="newSessionLabel wide"
-      >
-        新会话
-      </span>
-    </button>
-    <div
-      class="regionArea"
-    >
       <div
-        data-slot="sidebar.workspaces"
-        style="display: contents;"
-      />
-    </div>
-    <div
-      class="footArea"
-    >
-      <div
-        class="footerActions"
+        class="regionArea"
       >
         <div
-          data-slot="sidebar.footer.action"
+          data-slot="sidebar.workspaces"
           style="display: contents;"
         />
       </div>
       <div
-        class="settingsArea"
+        class="footerActions"
       >
         <div
-          data-slot="sidebar.settings"
+          data-slot="sidebar.footer.action"
           style="display: contents;"
         />
       </div>
     </div>
+    <div
+      class="settingsArea"
+    >
+      <div
+        data-slot="sidebar.settings"
+        style="display: contents;"
+      />
+    </div>
   </div>
 </div>
 `;
diff --git a/packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx b/packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx
index 8f53c92a83..667461eeca 100644
--- a/packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx
+++ b/packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx
@@ -96,6 +96,18 @@ describe('SidebarRoot shell', () => {
     expect(b.toggleSidebar).not.toHaveBeenCalled()
   })
 
+  it('groups every non-settings action above the independent Settings seat', () => {
+    mountShell()
+    const region = screen.getByTestId('region')
+    const footer = screen.getByTestId('footer-action-seat')
+    const settings = screen.getByTestId('settings-seat')
+    const root = region.closest('[class*="root"]')!
+    const primary = region.closest('[class*="primaryArea"]')!
+    expect(primary.contains(footer)).toBe(true)
+    expect(primary.contains(settings)).toBe(false)
+    expect(settings.parentElement?.parentElement).toBe(root)
+  })
+
   it('keeps the region mounted through collapse and expands on its request', () => {
     vi.useFakeTimers()
     const b = mountShell()
diff --git a/packages/client/ui-sidebar/tests/sidebar-styles.client.spec.ts b/packages/client/ui-sidebar/tests/sidebar-styles.client.spec.ts
index 19e7a54380..daa793df65 100644
--- a/packages/client/ui-sidebar/tests/sidebar-styles.client.spec.ts
+++ b/packages/client/ui-sidebar/tests/sidebar-styles.client.spec.ts
@@ -40,16 +40,17 @@ describe('SidebarRoot.module.css', () => {
     expect(declarations('.collapsed .regionArea')?.get('margin-right')).toBe('0')
   })
 
-  it('moves the four upper controls while the settings seat only fades', () => {
+  it('moves every upper-group control while the settings seat only fades', () => {
     const animation = 'rail-in 150ms var(--ds-ease-in-out) backwards'
     for (const selector of [
       '.railIn .iconButton',
       '.railIn .newSession',
       '.railIn .regionArea',
+      '.railIn .footerActions',
     ]) {
       expect(declarations(selector)?.get('animation')).toBe(animation)
     }
-    expect(declarations('.railIn .footArea')?.get('animation')).toBe(
+    expect(declarations('.railIn .settingsArea')?.get('animation')).toBe(
       'rail-fade-in 150ms var(--ds-ease-in-out) backwards',
     )
     expect(css).toMatch(
@@ -63,4 +64,12 @@ describe('SidebarRoot.module.css', () => {
     expect(declarations('.collapsed .newSession')?.get('align-self')).toBe('flex-start')
     expect(declarations('.collapsed .newSession')?.get('width')).toBe('36px')
   })
+
+  it('defines separate narrow floating surfaces for functions and Settings', () => {
+    const surfaces = declarations('.collapsed .primaryArea')
+    expect(surfaces?.get('width')).toBe('48px')
+    expect(surfaces?.get('background')).toBe('var(--dsw-specific-sidebar-fill)')
+    expect(css).toContain('@media (max-width: 1023px)')
+    expect(css).toMatch(/\.collapsed \.settingsArea\s*\{\s*margin-top: auto;/)
+  })
 })
